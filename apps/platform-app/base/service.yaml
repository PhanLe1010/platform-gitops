apiVersion: v1
kind: Service
metadata:
  name: platform-app
  labels:
    app: platform-app
spec:
  type: ClusterIP
  selector:
    app: platform-app
  ports:
    - name: http
      port: 80
      targetPort: http